<%- include('partials/header') %>

<div class="max-w-4xl mx-auto">
    <h2 class="text-2xl font-bold mb-6">財務健康度輸入</h2>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="bg-cardbg p-6 rounded-xl border border-gray-700 shadow-lg">
            <form action="/result" method="POST" class="space-y-6">
                <div>
                    <label class="block text-sm font-medium text-gray-400 mb-2">月收入 (萬元)</label>
                    <input type="number" name="income" class="w-full bg-darkbg border border-gray-600 rounded-lg px-4 py-3 text-white focus:ring-2 focus:ring-primary focus:border-transparent outline-none transition" placeholder="例如：5" required>
                </div>
                <div>
                    <label class="block text-sm font-medium text-gray-400 mb-2">總負債 (萬元)</label>
                    <input type="number" name="debt" class="w-full bg-darkbg border border-gray-600 rounded-lg px-4 py-3 text-white focus:ring-2 focus:ring-primary focus:border-transparent outline-none transition" placeholder="例如：200" required>
                </div>
                <button type="submit" class="w-full bg-gradient-to-r from-blue-600 to-blue-500 hover:from-blue-500 hover:to-blue-400 text-white font-bold py-3 rounded-lg shadow-lg transform transition hover:scale-[1.02]">
                    <i class="fa-solid fa-robot mr-2"></i> 開始 AI 分析
                </button>
            </form>
        </div>

        <div class="bg-cardbg p-6 rounded-xl border border-gray-700 shadow-lg flex flex-col justify-center text-center">
            <div class="text-6xl text-gray-600 mb-4">
                <i class="fa-solid fa-chart-pie"></i>
            </div>
            <h3 class="text-xl font-semibold mb-2">為什麼要檢測？</h3>
            <p class="text-gray-400">透過融數科技的 RAG 算法，我們能精準評估您的負債風險，並提供法律上的建議方案。</p>
        </div>
    </div>
</div>

<%- include('partials/footer') %>
