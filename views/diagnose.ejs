<%- include('partials/header') %>
<div class="max-w-4xl mx-auto">
    <div class="flex items-center mb-6">
        <div class="w-1 h-8 bg-blue-500 mr-3"></div>
        <h2 class="text-2xl font-bold tracking-wide">輸入演算參數 (Input Parameters)</h2>
    </div>
    
    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="bg-cardbg p-6 rounded-xl border border-gray-700 shadow-lg relative overflow-hidden">
            <div class="absolute top-0 right-0 w-20 h-20 bg-blue-500/5 rounded-bl-full"></div>
            
            <form action="/result" method="POST" class="space-y-6 relative z-10">
                <div>
                    <label class="block text-xs font-mono text-blue-400 mb-2 uppercase">Monthly Income (Unit: 10k)</label>
                    <input type="number" name="income" class="w-full bg-darkbg border border-gray-600 rounded px-4 py-3 text-white focus:ring-1 focus:ring-blue-500 focus:border-blue-500 outline-none font-mono" placeholder="INPUT_VALUE..." required>
                </div>
                <div>
                    <label class="block text-xs font-mono text-blue-400 mb-2 uppercase">Total Debt (Unit: 10k)</label>
                    <input type="number" name="debt" class="w-full bg-darkbg border border-gray-600 rounded px-4 py-3 text-white focus:ring-1 focus:ring-blue-500 focus:border-blue-500 outline-none font-mono" placeholder="INPUT_VALUE..." required>
                </div>
                <button type="submit" class="w-full bg-blue-600 hover:bg-blue-500 text-white font-bold py-3 rounded shadow-[0_0_15px_rgba(37,99,235,0.4)] transition border border-blue-400/50 flex justify-center items-center group">
                    <i class="fa-solid fa-microchip mr-2 group-hover:animate-pulse"></i> 啟動運算引擎 (INITIALIZE)
                </button>
            </form>
        </div>
        
        <div class="bg-cardbg p-6 rounded-xl border border-gray-700 shadow-lg flex flex-col justify-center text-center relative">
            <div class="absolute inset-0 bg-[url('https://www.transparenttextures.com/patterns/carbon-fibre.png')] opacity-10"></div>
            <div class="text-6xl text-gray-700 mb-4 relative z-10"><i class="fa-solid fa-network-wired"></i></div>
            <h3 class="text-xl font-semibold mb-2 text-gray-200 relative z-10">邏輯矩陣解析</h3>
            <p class="text-gray-400 text-sm relative z-10">系統將透過 RAG 檢索增強生成技術，比對法律條文與財務模型，輸出最佳化策略。</p>
        </div>
    </div>
</div>
<%- include('partials/footer') %>
